{
  "command": "update-params",
  "description": "Update Treasury parameters",
  "parameters": [
    {
      "name": "address",
      "type": "string",
      "required": true,
      "format": "verona-address",
      "description": "Treasury contract address"
    },
    {
      "name": "redirect-url",
      "type": "string",
      "required": false,
      "format": "url",
      "description": "OAuth redirect URL"
    },
    {
      "name": "icon-url",
      "type": "string",
      "required": false,
      "format": "url",
      "description": "Treasury icon URL"
    },
    {
      "name": "metadata",
      "type": "json",
      "required": false,
      "description": "Metadata as JSON string"
    },
    {
      "name": "network",
      "type": "enum",
      "required": false,
      "enum": ["local", "testnet", "mainnet"],
      "default": "testnet",
      "description": "Network to use"
    }
  ],
  "validation_rules": [
    "At least one parameter to update is required (--redirect-url, --icon-url, or --metadata)",
    "After changing redirect_url or icon_url via update-params, run: verona-toolkit oauth2 client sync-from-treasury <client_id> (verona-oauth2-client)"
  ],
  "examples": [
    {
      "description": "Update redirect URL (then sync OAuth client)",
      "workflow": [
        "verona-toolkit treasury update-params xion1abc... --redirect-url https://example.com/callback",
        "verona-toolkit oauth2 client sync-from-treasury client_abc123"
      ],
      "params": {
        "address": "xion1abc...",
        "redirect-url": "https://example.com/callback"
      }
    },
    {
      "description": "Update multiple parameters",
      "params": {
        "address": "xion1abc...",
        "redirect-url": "https://app.com/callback",
        "icon-url": "https://app.com/icon.png"
      }
    }
  ]
}